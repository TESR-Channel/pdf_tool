# TESR PDF Editor

PDF Editor สำหรับทีม TESR Co., Ltd. — ทำงานบน browser 100% (ไม่มี server, ไฟล์ไม่ออกจากเครื่อง)
รองรับมือถือ / แท็บเล็ต / คอมพิวเตอร์ และติดตั้งเป็นแอปบนหน้าจอโฮมได้ (PWA manifest)

## วิธีใช้
1. วางไฟล์ PDF (หรือรูป JPG/PNG — จะถูกแปลงเป็นหน้า PDF) ที่หน้าแรก เลือกหลายไฟล์ได้ = รวมไฟล์
2. แก้ไขใน editor ด้วย toolbar ด้านบน แล้วกด **ดาวน์โหลด** หรือ **พิมพ์**

## หน้าตาแบบ Acrobat
- แถบเมนู **เครื่องมือทั้งหมด / แก้ไข / แปลง / ลงนาม** สลับชุด toolbar + ช่อง **ค้นหาข้อความในเอกสาร** (Enter) พร้อม highlight และกดไปยังผลลัพธ์
- Panel ซ้าย: แท็บ **หน้า** (thumbnail) และ **เครื่องมือทั้งหมด** (รายการแบบ Acrobat)
- Rail ขวา: เลขหน้า / เลื่อนหน้า / หมุน / ซูม

## เครื่องมือใน editor
| กลุ่ม | เครื่องมือ |
|---|---|
| ใส่บนหน้า | ข้อความ (ไทยได้) · ไฮไลท์ · ปากกา · ยางลบ/ปิดทับ · รูปทรง · รูปภาพ · **หมายเหตุ** (บันทึกเป็น PDF Comment จริง) · **ปกปิดข้อมูล/Redact** (ลบเนื้อหาใต้กรอบออกจากไฟล์จริง) |
| ลงนาม | ลายเซ็น (วาด/อัปโหลด) · **ตราประทับ** (อนุมัติ/ตรวจแล้ว/ลับ/… หรือพิมพ์เอง) · ใส่วันที่ภาษาไทย |
| ทั้งเอกสาร | ลายน้ำ (ข้อความ / โลโก้ TESR / รูป) · เลขหน้า |
| จัดหน้า (แถบซ้าย) | ลากสลับลำดับ · หมุน · ทำสำเนา · ลบ · เพิ่มหน้าจากไฟล์อื่น · หน้าว่าง |
| แปลง | PDF→JPG/PNG · PDF→TXT · **PDF→Word (.docx)** · **OCR ไทย/อังกฤษ** (Tesseract.js โหลดเมื่อใช้) ทำ PDF สแกนให้ค้นหา/คัดลอกได้ · ลดขนาด · แยกไฟล์ |
| ไฟล์ | ดาวน์โหลด · **แชร์** (Web Share บนมือถือ → LINE/อีเมล) · พิมพ์ · Metadata |
| อื่นๆ | เลิกทำ/ทำซ้ำ (Ctrl+Z / Ctrl+Y) · ซูม · พิมพ์ · ลากไฟล์มาวางใน editor เพื่อรวมไฟล์ |

ทุกวัตถุที่ใส่ลงไป (รวมลายน้ำ/เลขหน้า) เลือก ย้าย ปรับขนาด แก้ไข หรือลบได้ก่อนดาวน์โหลด

## Deploy บน GitHub Pages
1. สร้าง repo แล้วอัปโหลดไฟล์ทั้งหมดในโฟลเดอร์นี้ไปที่ branch `main`
2. **Settings → Pages → Source: Deploy from a branch → main / (root)** → Save
3. เปิดที่ `https://<org>.github.io/<repo>/`

เปิดไฟล์จาก URL ได้ด้วย `?file=path/to/file.pdf` (ไฟล์ต้องอยู่ใน repo เดียวกันหรืออนุญาต CORS)

## โลโก้
`logo.png` ใช้ใน header, หน้าแรก, favicon, manifest และตัวเลือก "โลโก้ TESR" ในลายน้ำ

## Stack
pdf-lib + @pdf-lib/fontkit (สร้าง/แก้ PDF, ฝังฟอนต์ไทย Loma สำหรับ OCR text layer) · PDF.js · JSZip · Tesseract.js (OCR, โหลดจาก CDN เมื่อกดใช้) · Google Fonts Kanit + Sarabun
ข้อความที่ใส่ถูกเรนเดอร์ผ่าน canvas จึงรองรับภาษาไทยและทุกฟอนต์

## ข้อจำกัด
- ยังใส่รหัสผ่าน/เข้ารหัส PDF ไม่ได้ (pdf-lib ไม่รองรับ) และไม่มี e-signature แบบขอลงนามผ่านอีเมล (ต้องมี server)
- OCR ครั้งแรกจะดาวน์โหลดข้อมูลภาษา ~10 MB จาก CDN และใช้เวลาหลายวินาทีต่อหน้า
- แก้ "ข้อความเดิม" ใน PDF โดยตรงไม่ได้ — ใช้ยางลบปิดทับแล้วพิมพ์ทับแทน
- "ลดขนาด" แปลงหน้าเป็นภาพ ข้อความจะเลือก/ค้นหาไม่ได้
- ไฟล์ใหญ่มาก (>100 MB) อาจช้าบนมือถือ
